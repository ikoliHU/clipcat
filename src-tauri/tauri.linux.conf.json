{
  "$schema": "https://schema.tauri.app/config/2",
  "bundle": {
    "targets": ["deb", "rpm", "appimage"],
    "linux": {
      "deb": {
        "depends": ["obs-studio", "ffmpeg"]
      },
      "rpm": {
        "depends": ["obs-studio", "/usr/bin/ffmpeg"]
      }
    }
  }
}
